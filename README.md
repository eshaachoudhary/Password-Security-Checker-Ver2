# Password-Security-Checker-Ver2
**Password Security Checker** — A Python CLI tool that audits password strength using length, character variety, entropy estimation, and pattern detection (repeated/sequential characters), then cross-checks the password against known data breaches via the Have I Been Pwned API using k-anonymity, so the real password never leaves the device. 
